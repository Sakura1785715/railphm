# scripts/archive

本目录用于归档历史排查、临时实验和一次性诊断脚本。

这些脚本不属于当前主实验流程，文件未删除，仅从 `scripts/` 根目录移入归档区，便于毕业设计过程溯源。后续如果确认需要恢复，可将对应脚本移回 `scripts/`。

当前主流程脚本仍保留在 `scripts/` 根目录。后续统一实验入口将优先调用 `scripts/` 根目录中的稳定脚本。

当前归档脚本：

- `run_task_5_3c_horizon_compare.sh`：Task 5-3c horizon compare 任务型 shell 包装脚本。
- `run_task_5_3d_feature_ablation_terminal.sh`：Task 5-3d feature ablation 任务型 shell 包装脚本。
